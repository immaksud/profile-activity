# Profile Activity

This repository was created to practice GitHub commits and maintain activity.

## About
A simple repository with an initial commit.

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/profile-activity.git
git push -u origin main

First update.